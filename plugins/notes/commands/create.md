---
description: Write a note into the current Obsidian vault, with frontmatter and folder placement.
argument-hint: --title "Note Title" --content "Note content" [--folder "Subfolder"] [--frontmatter '{"key":"value"}']
---

Invoke the `notes:create` skill via the Skill tool, passing through any arguments the user provided.

ARGUMENTS: $ARGUMENTS
