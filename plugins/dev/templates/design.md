---
type: design
status: draft
stage: design
size:
created: {{date:YYYY-MM-DD}}
work:
related: []
---

# <Topic> Design

## Context & Requirements

## Use Case Corpus

<!--
REQUIRED — fill this in BEFORE Decisions/Architecture. Do not design syntax,
AST, or schema until the use cases are enumerated.

Enumerate EVERY planned usage of the thing being designed — for a language
feature, the concrete call sites/rules authors will write; for an API, the
call sites; for a schema, the queries. Ground each in the real source (corpus
entry, ticket, existing code), not invented examples.

Then add a coverage check: validate the proposed surface (grammar/API/schema
in §Architecture) against EACH use case. The rows that DON'T cleanly map are
your most valuable findings — they are the gaps that would otherwise ship.

A design without its use case corpus is designed blind. This section is the
source of truth the rest of the document must satisfy.
-->

## Decisions

## Architecture

## Testing Strategy

## Milestones
