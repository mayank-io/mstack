---
description: Run iterative ML design review with 3 parallel reviewers (Principal ML Engineer, Sr Applied Scientist, ML Ops Engineer) and diminishing fix thresholds across up to 4 iterations.
argument-hint: Optional path to design doc (defaults to most recent in docs/)
---

Invoke the `dev:ml-design-review-iterative` skill via the Skill tool, passing through any arguments the user provided.

ARGUMENTS: $ARGUMENTS
