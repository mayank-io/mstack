---
description: Run iterative code review with 3 parallel reviewers (PE, Sr SDE, QA) and diminishing fix thresholds across up to 4 iterations.
argument-hint: Optional scope (file paths, branch name, or "staged")
---

Invoke the `dev:code-review-iterative` skill via the Skill tool, passing through any arguments the user provided.

ARGUMENTS: $ARGUMENTS
