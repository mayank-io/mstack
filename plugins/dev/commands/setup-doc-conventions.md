---
description: Bootstrap doc-conventions in the current project. Creates docs/templates/ (including dashboard.base), docs/dashboard.md, configures Obsidian, and appends workflow section to CLAUDE.md.
---

Invoke the `dev:setup-doc-conventions` skill via the Skill tool, passing through any arguments the user provided.

ARGUMENTS: $ARGUMENTS
