---
description: Run iterative design document review with 3 parallel reviewers (PE, Sr SDE, Domain Expert) and diminishing fix thresholds across up to 4 iterations.
argument-hint: Optional path to design doc (defaults to most recent in docs/)
---

Invoke the `dev:design-review-iterative` skill via the Skill tool, passing through any arguments the user provided.

ARGUMENTS: $ARGUMENTS
