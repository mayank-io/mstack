---
description: Extract content from an X/Twitter post, thread, or article using the gstack browser. Detects threads automatically and downloads images locally.
argument-hint: <X URL> [output_dir]
---

Invoke the `fetch:x-post` skill via the Skill tool, passing through any arguments the user provided.

ARGUMENTS: $ARGUMENTS
