---
description: Download all pages from a public Notion site as Markdown files with cross-references
argument-hint: <Notion URL> [output_dir] [--test]
---

Invoke the `fetch:notion-public-site` skill via the Skill tool, passing through any arguments the user provided.

ARGUMENTS: $ARGUMENTS
