---
description: Clean up the permissions allow list in settings.json by removing one-off pastes, redundant entries, and dead MCP references
---

Invoke the `ccimprove:clean-permissions` skill via the Skill tool, passing through any arguments the user provided.

ARGUMENTS: $ARGUMENTS
