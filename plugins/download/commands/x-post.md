---
description: Extract content from an X/Twitter post, thread, or article using Playwright. Detects threads automatically and downloads images locally.
argument-hint: <X URL> [download_dir]
---

Invoke the `download:x-post` skill via the Skill tool, passing through any arguments the user provided.

ARGUMENTS: $ARGUMENTS
